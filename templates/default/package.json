{
  "name": "{{kebab name}}",
  "version": "0.0.1",
  "private": true,
  "description": "{{description}}",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "{{contact}}",
  "license": "{{license}}",
  "devDependencies": {
    "parcel": "^2.5.0"
  },
  "dependencies": {
    "@ineka/engine": "^2.0.0"
  }
}
