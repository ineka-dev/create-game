{
  "name": "{{kebab name}}",
  "version": "0.0.1",
  "private": true,
  "description": "{{description}}",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "author": "{{contact}}",
  "license": "{{license}}",
  "devDependencies": {
    "typescript": "^5.1.6",
    "vite": "^4.4.2"
  },
  "dependencies": {
    "@ineka/engine": "latest"
  }
}